{
  "name": "spektif-agency",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:seed": "firebase functions:shell --only seedDatabase",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "firebase:init": "firebase init",
    "firebase:login": "firebase login",
    "firebase:deploy": "firebase deploy",
    "firebase:emulators": "firebase emulators:start",
    "firebase:migrate": "node scripts/migrate-to-firebase.js"
  },
  "devDependencies": {
    "@turbo/gen": "^1.13.0",
    "prettier": "^3.0.0",
    "turbo": "^1.13.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.0.0",
  "dependencies": {}
}
